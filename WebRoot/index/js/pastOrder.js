$(function(){
	/*var total;//订单总数量
	var currentPage=1;//当前第几页
	var offset = 20;
	var limit = 20;//每页显示数量
	var order = "";//排序规则，0默认距离排序，1字母排序，2价格排序，3评分排序
	
	function initPaginatior(){
		var options = {
				currentPage: 1,
				totalPages: 10,
				numberOfPages:5,
				size:'normal',
				alignment:'right',
				onPageClicked: function(event, originalEvent, type, page){
					$('#alert-content').text("Page item clicked, type: "+type+" page: "+page);
					alert("type:" + type +"page:"+page);
					currentPage = page;
					
				}
		}
		
		$('#paging').bootstrapPaginator(options);
		
	}
	
	initPaginatior();
	alert()
	
	function getorders( ){
		$.ajax({
			type:'post',
			url: apppath+'/consumers/getCourrentPageOrders',
			data: {offset:offset, limit:limit, sort: order:},
			
			
			
		})
		
	}*/
	//private int offset;//current page 偏移数据量
	//private int limit;//rows of per page 每页显示记录数
	//private String sort;//the sort clumn 排序字段
	//private String order;// asc/desc
	//private String search; // search key words 搜索框查询的关键字
	
	
	
	
	
	
	
	
});